# FAILURE LOG: Risk Tracking & Lessons Learned

| 日付 | フェーズ | 失敗/リスクの内容 | 原因 | 対策・設計変更 |
| :--- | :--- | :--- | :--- | :--- |
| 2024-05-22 | 初期設計 | AIによるコード出力の省略 | トークン節約の自動化 | `PRINCIPLE.md` に省略禁止条項を追加。 |
| 2024-05-22 | 初期設計 | 開発文脈の消失 | セッション切断 | `HANDOVER.md` の常時更新ルールを新設。 |
| 2024-05-22 | 初期設計 | 計画と実装の乖離(Drift) | 実装時の無意識な仕様変更 | Step 6 に [ALIGNMENT CHECK] を追加。 |
| 2024-05-22 | 構成設計 | フォルダ構成の不透明さ | 物理配置の定義不足 | `DEVELOPMENT_RULES.md` にディレクトリマップを定義。 |
| 2026-04-08 | 設定設計 | 閾値設定の不整合リスク | 共通認識の欠如 | `settings.json.template` 作成。 |
| 2026-04-08 | ドキュメント | README と Design の重複 | 役割定義の曖昧さ | README を要件(What)、Design を仕様(How)に分離。 |
| 2026-04-08 | モデル選定 | Filter モデルが旧世代(1.5 Flash)でコスト優位性が不十分 | モデル世代の見直し欠如 | Filter を **Gemini 2.5 Flash Lite** に変更、thinking抑制とJSON強制を明記。 |
| 2026-04-08 | 要件拡張 | ニュース特化設計では他用途に転用不能 | 汎用性要件の欠落 | レベル2抽象化(パイプライン定義型)を採用、`src/apps/` で用途分離。 |
| 2026-04-08 | 要件拡張 | ステージ間実行制御・状態管理・観測性が未定義 | オーケストレーション層の欠落 | `src/orchestrator/` を新設し機能 A/B/C/E を SYSTEM_DESIGN に追加。D は将来拡張。 |
| 2026-04-08 | 運用設計 | 常駐サーバ導入でコスト最小化原則に違反する懸念 | 実行形態の未確定 | GitHub Actions cron による都度起動型を採用、常駐禁止を PRINCIPLE に明記。 |
| 2026-04-08 | Phase4 → 5 | `parallel: true / fan_out` 宣言が pipeline.yml に存在しても DAG エンジンが解釈せず単発実行していた | エンジン側の宣言→実装の乖離。`StageSpec` に値は載っていたが `DagExecutor` 側がそれを参照する経路が無かった | `_invoke_fanout` を新設し、`payload[fan_out]` を要素単位で `ThreadPoolExecutor` 並列実行 → 結果を連結し aggregate `score=max(...)` を再構築する経路を engine 内に追加。`when:` 式の後方互換維持を契約化。 |
| 2026-04-08 | Phase4 → 5 | dedupe ステージの sliding-window seen_set がジョブ跨ぎで失われる | チェックポイント書込責務がステージ内に閉じていた(`keyword.py` 自前)。エンジンが checkpoint 概念を知らなかった | `pipeline.schema.json` の `checkpoint: true` デフォルトに合わせ、`DagExecutor._save_checkpoint` フックを新設(成功時に payload を `state.save_checkpoint(stage.id)` で書き出す)。`keyword.py` 内の self-checkpoint は別キー(`seen_items::<app>`)で共存させる選択。 |
| 2026-04-08 | Phase5 → 6 | scheduler が Firestore に `job_runs` のみ書き、`stage_runs` を書かないため Admin SPA の Stages ビューが永遠に空になる | DagExecutor が `StateStore` に対して checkpoint 以外の lifecycle メソッドを知らなかった | `FirestoreStateStore` に `record_stage_run_start/finish` の duck-typed メソッドを追加し、`DagExecutor._run_stage` の各 attempt 開始/終了で `getattr(state, "record_stage_run_*", None)` 経由でベストエフォート呼出。Protocol を肥大化させずに lifecycle を engine から書き込めるようにした。 |
| 2026-04-08 | Phase5 → 6 | `MetricsSink` が flush() 時にバッファ書込していたため、ジョブ実行中に走る `meta_export` ステージが metrics コレクションを空のまま読んでしまう | バッファリングは Firestore コスト最適化のつもりだったが、Firestore は per-write 課金で実際は意味がなく、観測性の遅延だけが残っていた | write-through 方式に変更(`record()` で即時書込、`flush()` は no-op)。`meta_export` がジョブ実行中の最新コストを読み取れるようになった。 |
| 2026-04-08 | Phase6 | **全ステージで `ctx.logger.info("msg", extras={...})` と書かれていたが、Python 標準 `logging.Logger.info` は `extras=` を受け付けず `TypeError` を送出**。Phase 3〜5 のテストは全て NoopLogger(`info=warning=error=lambda *a,**k:None`)で kwargs を黙殺していたため検出できず、Phase 6 で初めて real Firestore 統合テストが本物の構造化 logger を通したことで顕在化。エラーは keyword.py 内の try/except の外で発生 → dedupe が FAILED 返却 → filter 以降が「親失敗」で SKIP → research の when 評価で `filter` が outputs に存在せず `WhenExpressionError` で全体クラッシュ。 | (a) `contracts.Logger` Protocol が `**kwargs: Any` で曖昧に定義されていた。(b) Noop ロガーがエラーを隠すテストダブルとして機能していた(Mock anti-pattern)。 | (a) `observability.get_logger` を `_StageLogger` アダプタクラスでラップし、`extras=` を受け取って内部で `extra={"extras": {...}}` に翻訳してから stdlib logger に委譲。stage コードは無修正のまま動作。(b) Phase 6 統合テストは「real logger を通すパス」を必須化することで Mock anti-pattern の再発を防ぐ。`tests/test_firestore_integration.py` をリグレッションガードとして固定化。 |
| 2026-04-08 | Phase6 | `r2_upload` が複数 reporter からの artifact を `payload["artifact"]`(単数)のみ参照していたため `meta_export` の出力(`payload["artifacts"]` リスト)を取りこぼしていた | reporter インタフェースの暗黙契約が「単一 artifact 返却」だったが、新規 reporter で複数を返したくなった際に upstream 集約側が追従していなかった | `r2_upload._collect_artifacts` を `artifact` 単数 + `artifacts` 複数の両対応に拡張。R2 キー生成も `local_path.name` から `artifacts_root` 相対パスに変更し、サブディレクトリ(`meta/`)構造を保持。 |
| 2026-04-08 | Phase6 → 7 | **「Cloudflare にデプロイできない」状態が出荷直前まで気付かれていなかった**。`docs/HOSTING.md` には「`wrangler deploy` を手で打て」と書いてあったが、これは AI-Only Operations 原則(人間の手作業ゼロ)に違反。さらに `CLOUDFLARE_R2_KEY/SECRET`(S3 互換データ平面キー)と `CLOUDFLARE_API_TOKEN`(Worker / Pages 管理平面の bearer token)が混同されており、実は wrangler を動かすための credential が secrets に1つも入っていなかった。 | (a) Phase 4 で Worker / wrangler.toml を作った時点で「デプロイは別の secrets が要る」という認識が欠落。(b) 同一プロバイダで2種類の credential を使うパターン(データ平面 vs 管理平面)を整理した文書が無かった。(c) ユーザの「実運用できるか」観点での監査が行われていなかった。 | (a) `.github/workflows/deploy-cloudflare.yml` を新設し preflight → buckets → worker → pages の4ジョブで完全自動化。(b) `docs/HOSTING.md §0` に **ランタイム secrets(8 個)とデプロイ secrets(2 個)を完全分離** した一覧表を追加し、「同じプロバイダでも目的別に異なる credential」と明記。(c) 教訓: 新コンポーネントを追加した時点で「これを本番に出すために必要な credential / 自動化ステップは何か」を必ず棚卸しする。 |
